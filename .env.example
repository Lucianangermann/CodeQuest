# Optional — only needed for AI hints/explanations/chat
# Without this key the app still works; AI features show a friendly "not configured" message.
ANTHROPIC_API_KEY=sk-ant-...

# Optional — change this to a random string in production
# JWT_SECRET=change-me-in-production
